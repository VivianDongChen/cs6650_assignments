# Redis缓存配置

## Redis服务器配置
# Redis主机地址（默认: localhost）
REDIS_HOST=localhost

# Redis端口（默认: 6379）
REDIS_PORT=6379

# Redis认证密码（可选，默认: 空）
REDIS_PASSWORD=

# Redis数据库编号（默认: 0）
REDIS_DB=0

## 缓存策略配置

# 聚合查询缓存TTL（秒）- getTotalMessages()等
CACHE_TTL_METRICS=600

# 热数据查询缓存TTL（秒）- getTopActiveUsers(), getTopActiveRooms()
CACHE_TTL_TOP_USERS=180
CACHE_TTL_TOP_ROOMS=180

# 小时分布统计缓存TTL（秒）
CACHE_TTL_HOURLY=3600

# 按房间统计缓存TTL（秒）
CACHE_TTL_BY_ROOM=300

## 缓存维护任务配置

# 缓存刷新间隔（分钟）- 多久清除一次过期缓存
CACHE_REFRESH_INTERVAL=5

# 缓存统计日志间隔（秒）- 多久输出一次缓存命中率
CACHE_STATS_LOG_INTERVAL=30

# Redis健康检查间隔（秒）
REDIS_HEALTH_CHECK_INTERVAL=60

## 示例环境变量设置

# 本地开发环境
# export REDIS_HOST=localhost
# export REDIS_PORT=6379
# export REDIS_DB=0

# AWS ElastiCache环境
# export REDIS_HOST=your-elasticache-endpoint.cache.amazonaws.com
# export REDIS_PORT=6379
# export REDIS_PASSWORD=your-auth-token
# export REDIS_DB=0
